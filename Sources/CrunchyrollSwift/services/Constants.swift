import Foundation

extension CRAPIService {
    public enum Constants {
      static let BaseURL = URL(string: "https://api.crunchyroll.com/")!
      static let crUnblockerURL = URL(string: "https://cr-unblocker.us.to")!
    }
}
