import Foundation

extension CRAPIService {
    public enum Constants {
      static let BASE_URL = URL(string: "https://api.crunchyroll.com/")!
      static let CR_UNBLOCKER_URL = URL(string: "https://cr-unblocker.us.to")!
    }
}
